# 掌柜助手 · 定制开发文档

版本：v1.1 | 定制对象：个人卖家（一人多店多账号） | 平台：拼多多 + 闲鱼
核心理念：**简单化操作 —— 一句话、三步内完成日常运营动作**

v1.1 变更：恢复多账号支持；新增模型接入层（LLM/生图可插拔，支持 OpenAI/DeepSeek/智谱/百炼及任意自定义端点）；新增设置页、账号健康面板、用量统计、数据备份与日志导出。

---

## 1. 项目定位

- 给朋友一人定制的双平台运营工具，非商业化 SaaS；
- 用户画像：个人卖家 / 兼职副业，**不懂电商后台、不想学**，双平台多个账号（闲鱼多号 + 拼多多店铺）；
- 替代方案：闲管家（198元/年，30 账号）+ 阿奇索（138元/年+奇豆按次扣费）+ 手动后台操作；
- 本项目优势：免费、本地数据、自然语言驱动、模型 API 自由换、按朋友经营品类预置模板。

**明确不做**：客服自动回复、资金类操作（支付/退款）、多平台扩展（抖音/淘宝）、仓储/财务 ERP。

---

## 2. 市场调研结论与功能取舍

### 2.1 竞品功能矩阵

| 软件 | 定位 | 核心功能 | 收费模式 |
| --- | --- | --- | --- |
| 闲管家 | 闲鱼工作台 | 多账号（30个）、自动擦亮（定时/重新发布）、售罄自动上架、定时发布、批量降价、商品搬家、IM客服 | 198元/年 |
| 阿奇索 | 多平台自动发货 | 多店铺切换、聊天窗口发货、卡密直充、自动售后、批量上架 | 138元/年 + 奇豆按次计费 |
| 快鱼助手Pro | 闲鱼采集上架 | RPA 采集→去水印→AI文案→批量上架一条龙 | 订阅制 |
| 妙手/店小宝类 | 铺货 ERP | 表格模板批量上架、批量编辑、多平台刊登、库存同步、数据报表 | SaaS 订阅 |
| 拼多多官方后台 | 商家后台 | 上架、订单、付费推广（ROI 报表） | 免费 |

### 2.2 取舍决策

| 功能 | 决策 | 理由 |
| --- | --- | --- |
| 一键上架（单品+批量） | ✅ 做（P0） | 所有竞品的入口功能，高频刚需 |
| 表格模板批量上架 | ✅ 做（P0） | ERP 类软件精髓，小白用 Excel 就能批量 |
| AI 文案 + 一键做图 | ✅ 做（P0） | 快鱼的核心卖点，闲鱼"卖图片"点击率定生死 |
| **多账号管理** | ✅ 做（P0） | 用户明确需要：闲鱼多号 + 拼多多多店，账号切换/隔离/批量 |
| **模型 API 可插拔** | ✅ 做（P0） | 用户明确需要：LLM 与生图供应商自由替换，不被单一厂商绑定 |
| 自动擦亮（闲鱼） | ✅ 做（P0） | 闲管家最受关注功能，闲鱼曝光刚需，定时任务实现 |
| 售罄自动上架（闲鱼） | ✅ 做（P1） | 闲管家热门功能，解决闲鱼库存=1 的痛点 |
| 抓取比价+定价建议 | ✅ 做（P0） | 差异化卖点，竞品基本没有 |
| 投流报表+调价建议（拼多多） | ✅ 做（P1） | 朋友有投流需求，规则引擎实现 |
| 订单查询/发货 | ✅ 做（P0） | 刚需，但只做查询+确认发货 |
| 数据导出+经营日报 | ✅ 做（P1） | 全部本地 CSV，Excel 直开；多账号分店+汇总两版 |
| **设置页（Web 可视化配置）** | ✅ 做（P0） | v1.0 缺陷：让小白手改 .env 不现实 |
| **账号健康面板** | ✅ 做（P1） | 多账号下 Cookie 状态一目了然，过期红点+续期引导 |
| **LLM 用量统计** | ✅ 做（P1） | v1.0 缺陷：API 按 token 计费，花费要可见 |
| **数据备份+日志一键导出** | ✅ 做（P1） | v1.0 缺陷：远程排障与数据安全 |
| 商品搬家（跨平台采集复制） | ⏸️ v2 | 有风险且非首期刚需，比价抓取已覆盖采集能力 |
| 自动发货/卡密 | ❌ 不做 | 朋友卖实物，不做虚拟商品 |
| IM 客服/智能回复 | ❌ 不做 | 需求文档明确：客服人工回复 |
| 仓储/财务 ERP | ❌ 不做 | 过度设计 |

---

## 3. 简单化操作设计（核心章节）

### 3.1 三条使用路径，按熟练度自选

| 路径 | 适合 | 示例 |
| --- | --- | --- |
| 对话式（默认） | 完全小白 | 输入"帮我看看保温杯卖多少钱合适"→ 直接出比价+建议价 |
| 功能卡片 | 半熟练 | 侧栏点"智能上架"→ 表单填空 → 预览 → 确认 |
| CLI/表格 | 熟练后 | `ecom xianyu listing batch items.csv --account xy01`，或丢一张 Excel 批量上架 |

### 3.2 五条简化铁律（贯穿所有模块）

1. **三步上限**：任何常用动作 ≤3 次交互完成（输入→预览→确认）；
2. **先预览后执行**：上架/发货/调价/建计划一律先出预览卡片，确认后才触达平台；
3. **模板填空**：不学后台字段，只填"标题/价格/图片/目录"四项，其余全部默认值；
4. **失败有兜底**：LLM 挂→规则模板；AI 生图挂→本地海报；无凭据→体验模式示例数据；
5. **一句话能说清结果**：所有输出用中文结论先行（"建议卖 39.9 元，市场均价 42.3，毛利 55%"），数据放后面。

### 3.3 体验模式 / 实盘模式

- 不添加任何账号 = 体验模式：全部功能用内置示例数据跑通，零门槛试用；
- 添加账号（Web 设置页粘贴凭据）= 实盘模式：界面显示"实盘"徽章，高危操作确认框变红。

---

## 4. 系统架构

### 4.1 架构图

```
┌─────────────────────────────────────────────────────────┐
│ 入口层   Web(对话+卡片+账号切换) │ CLI │ 定时任务(擦亮/日报)   │
├─────────────────────────────────────────────────────────┤
│ 路由层   意图识别：账号 × 平台 × 功能 × 动作 + 参数抽取        │
├─────────────────────────────────────────────────────────┤
│ 功能层   listing │ creative │ pricing │ ads                │
│         orders │ categories │ export │ analytics           │
│         （8 大模块，跨平台跨账号复用同一语义）                 │
├─────────────────────────────────────────────────────────┤
│ 平台层   xianyu 适配器        │  pinduoduo 适配器             │
│         bind(account) 实例化  │  bind(account) 实例化         │
├─────────────────────────────────────────────────────────┤
│ 模型层   llm.js（OpenAI 兼容统一客户端）│ imagegen.js（生图）   │
├─────────────────────────────────────────────────────────┤
│ 数据层   data/ 本地文件（按账号隔离）  │  外部 API               │
└─────────────────────────────────────────────────────────┘
```

设计原则：**功能 / 平台 / 账号 / 模型四层正交**。
新增平台 = 新增适配器；新增账号 = accounts.json 加一条；换模型供应商 = 设置页改一个预设。

### 4.2 目录结构

```
ecom-agent/
├── server.js              # Web 服务入口（Node 标准库 http，零依赖）
├── cli.js                 # CLI：ecom <平台> <功能> <动作> [参数] [--account]
├── lib/
│   ├── router.js          # 意图路由（关键词规则，LLM 可选增强）
│   ├── confirm.js         # 高危操作确认（预览→confirm→执行→审计日志）
│   ├── jobs.js            # 定时任务：按账号注册擦亮/售罄上架/日报
│   ├── settings.js        # 设置读写：data/settings.json（.env 兼容合并）
│   ├── llm.js             # LLM 统一接入层（OpenAI 兼容协议，四预设+自定义）
│   ├── imagegen.js        # 生图统一接入层（百炼原生/兼容端点/关闭→海报兜底）
│   ├── accounts.js        # 账号管理：增删改查/健康检查/状态缓存
│   ├── functions/         # 功能分区（8 个模块，均不感知账号细节）
│   │   ├── listing.js     #   上架：文案生成/单品/批量/CSV模板
│   │   ├── creative.js    #   做图：本地海报(800x800) + AI生图兜底
│   │   ├── pricing.js     #   比价：抓取竞品/价格分布/建议价/毛利率
│   │   ├── ads.js         #   投流：报表/ROI规则调价建议（仅拼多多）
│   │   ├── orders.js      #   订单：查单/发货（需确认）
│   │   ├── categories.js  #   目录：预置+自定义，两平台分开
│   │   ├── export.js      #   导出：CSV 带 BOM，Excel 直开
│   │   └── analytics.js   #   日报：分账号+汇总两版
│   └── platforms/
│       ├── xianyu.js      #   mtop 适配器：bind(account) 后调用
│       └── pinduoduo.js   #   开放平台适配器：bind(account) 后调用
├── data/
│   ├── settings.json      # 模型/生图/智能体名配置（含 key，gitignore）
│   ├── accounts.json      # 账号列表与凭据（gitignore）
│   ├── categories.json    # 目录（xianyu / pdd 分开）
│   ├── jobs.json          # 定时任务配置与执行日志
│   ├── usage.json         # LLM/生图调用流水（时间/账号/模块/tokens）
│   ├── exports/           # CSV 导出
│   └── images/            # 生成的海报/AI图
├── templates/
│   └── 批量上架模板.csv     # 表格批量上架模板
└── .env                   # 兼容旧配置（settings.json 优先）
```

### 4.3 模型接入层（v1.1 新增，核心）

**设计要点：OpenAI、DeepSeek、智谱、百炼四家全部提供 OpenAI 兼容协议，所以只写一个兼容客户端 + 预设表，不做四个 SDK。**

LLM 预设（设置页下拉选择，或自定义）：

| 预设 | base_url | 默认模型 | 说明 |
| --- | --- | --- | --- |
| OpenAI | `https://api.openai.com/v1` | gpt-4o-mini | 需海外网络 |
| DeepSeek | `https://api.deepseek.com/v1` | deepseek-chat | 便宜，中文文案质量好 |
| 智谱 | `https://open.bigmodel.cn/api/paas/v4` | glm-4-flash | 有免费档 |
| 百炼 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus | 阿里系 |
| 自定义 | 任意 | 任意 | 兼容 OneAPI/SiliconFlow 等中转 |

- 每次调用带超时与重试（1 次），失败走规则兜底，**模型层挂掉不阻塞任何功能**；
- 用量落账：每次调用记 `{时间, 账号, 模块, tokens}` 到 usage.json，日报附带"本月模型花费估算"；
- 设置页提供"测试连通性"按钮，一键验证当前配置。

生图预设（同样可插拔）：

| 预设 | 协议 | 默认模型 |
| --- | --- | --- |
| 百炼 | DashScope 原生（wanx 不走兼容协议） | wanx2.1-t2i-turbo |
| OpenAI 兼容 | `/images/generations` 标准接口 | 按供应商填 |
| 关闭 | — | 只用本地海报，零成本 |

### 4.4 多账号设计（v1.1 新增）

**账号模型**（data/accounts.json）：

```json
[
  { "id": "xy01", "name": "闲鱼主号", "platform": "xianyu", "cookie": "...", "status": "ok", "cookieUpdatedAt": "..." },
  { "id": "pdd01", "name": "拼多多店", "platform": "pinduoduo", "clientId": "...", "accessToken": "...", "status": "ok" }
]
```

- **账号切换**：Web 顶栏下拉切换"当前账号"，侧栏/对话/导出均作用于当前账号；另有"全部账号"视图执行跨账号批量（逐账号顺序执行+间隔）；
- **适配器实例化**：所有平台方法先 `bind(accountId)`，功能模块永不直接碰凭据；
- **数据隔离**：商品/订单/导出/日报按账号目录分开（`data/exports/xy01/*.csv`），汇总报表单独生成；
- **定时任务按账号注册**：每个闲鱼号独立的擦亮计划与高峰时段；售罄自动上架逐账号监听；
- **账号健康面板**：Cookie 剩余有效期（闲鱼约 24h）、过期红点、一键"扫码续期"引导；每天首次访问自动体检一次；
- **风控（闲鱼多号重点）**：同机多号操作间隔加长（跨账号批量间隔 60-180s 随机）、擦亮时段错峰、不并发多号请求。注：多账号本身有平台风控风险，界面明确提示，由使用者自行把握号数；
- **CLI**：`--account xy01` 指定账号，缺省为默认账号。

### 4.5 平台适配器统一接口

```js
// 每个适配器暴露统一能力；平台不支持的能力显式声明，调用时返回
// "闲鱼无投流功能" 的明确提示，而非静默失败
module.exports = {
  name: 'xianyu',
  capabilities: ['publish', 'polish', 'relist', 'listGoods', 'listOrders', 'ship', 'marketSearch', 'multiAccount'],
  bind(accountId) { /* 返回绑定该账号凭据的实例 */ },
  marketSearch(keyword) {},   // 竞品抓取
  publish(item) {},           // 上架（含批量，内部拟人间隔）
  polish(itemId) {},          // 擦亮：保留原曝光的"原商品擦亮"模式
  relist(itemId) {},          // 售罄后库存=1重新上架
  listGoods() {}, listOrders() {}, ship(orderId, express) {},
}
```

平台差异显式化：

| 能力 | 闲鱼 | 拼多多 |
| --- | --- | --- |
| 上架 | mtop 逆向（Cookie 约 24h 过期，健康面板引导续期） | 开放平台 API |
| 多账号 | ✅ 多号（注意风控间隔） | ✅ 多店 |
| 擦亮/售罄上架 | ✅ | ❌（无此概念） |
| 投流 | ❌（显式提示） | ✅ |
| 市场抓取 | 自带 mtop 搜索 | 需外部端点 `PDD_MARKET_SEARCH_URL` |

---

## 5. 功能规格

### F1 智能上架（P0）

- **单品**：填四项（标题/价格/图片/目录）→ LLM 按平台风格生成完整文案 → 预览 → 确认发布；
- **批量**：下载 CSV 模板 → 填好丢回 → 逐条生成+预览汇总 → 一次确认，批量发布（间隔 30-90s 拟人；跨账号再叠加账号间隔）；
- **上架即投流**（仅拼多多）：上架表单里勾选"同步创建推广计划"，填预算+目标 ROI 两个数即可；
- 类目自动推荐（关键词匹配 categories.json），不翻类目树；
- 全程作用域：当前账号，或"全部账号"批量。

### F2 一条龙做图（P0）

- 输入标题+价格+卖点 → 本地生成 800×800 主图海报（零依赖模板拼接，不花钱）；
- 生图 API（4.3 预设，可换/可关）可选，失败自动回退本地海报；
- 批量模式下逐商品自动出图，不单独操作。

### F3 抓取比价 + 定价建议（P0）

- 关键词 → 抓取两平台竞品价格/销量 → 结论先行的一页报告：
  市场价格分布（低价/均价/高价）、建议价、毛利率（需录入成本）、LLM 一句话建议；
- 输出示例："建议定价 39.9，低于市场均价 5%，预计毛利 55%"——直接可抄作业。

### F4 自动擦亮 + 售罄上架（P0，闲鱼专属，按账号）

- 定时擦亮：每个闲鱼账号独立设置时段（默认高峰前，如 19:50/21:50），错峰执行，保留原商品曝光/想要数；
- 售罄自动上架：商品售出后自动以库存=1 重新上架，逐账号监听；
- 全部留执行日志（data/jobs.json），界面可查"今天每个号擦亮了几次、成功几次"。

### F5 投流优化（P1，仅拼多多，按店铺）

- 报表：各店铺计划花费/ROI 一页看完；
- 调价建议（规则引擎）：ROI < 目标 → 建议降价 20%；ROI > 1.5×目标 → 建议加价 10%；
- 一键按建议执行（需确认）。

### F6 订单发货（P0）

- 当前账号待发货列表 → 勾选 → 填单号 → 确认发货。无批量 API 时逐单执行+间隔。

### F7 目录/模板管理（P0）

- 预置朋友经营品类的常用目录（定制化：按实际品类初始化）；
- 自定义目录持久化，平台之间互不串扰。

### F8 数据导出 + 经营日报（P1）

- 导出：商品/订单/投流/比价 CSV（带 BOM，Excel 中文不乱码），按账号目录存放；
- 日报（每天 20:00）：**分账号 + 全账号汇总两版**：商品数/订单数/营收/广告花费/利润 + 模型 API 花费估算。

### F9 设置页（P0，v1.1 新增）

- 四个分区：模型（LLM 预设+key+测试按钮）、生图（预设+key+开关）、账号（增删改+健康状态）、通用（智能体名/高峰时段/备份）；
- 写入 data/settings.json，.env 启动时合并（settings 优先），老配置无痛迁移；
- **小白全程不需要打开任何配置文件**。

### F10 数据备份与日志导出（P1，v1.1 新增）

- 设置页一键"备份数据"：data/ 打包 zip 下载（含账号凭据，提示妥善保存）；
- 一键"导出诊断日志"：近 7 天运行日志+配置脱敏快照打 zip，朋友远程求助时发过来即可排障。

---

## 6. 数据设计（全部本地文件，无数据库）

| 文件 | 内容 |
| --- | --- |
| data/settings.json | 模型/生图配置（含 key）、智能体名（gitignore） |
| data/accounts.json | 账号列表与凭据（gitignore） |
| data/categories.json | `{ xianyu: [...], pinduoduo: [...] }` 目录 |
| data/jobs.json | 定时任务配置 + 每次执行结果日志 |
| data/usage.json | 模型调用流水（时间/账号/模块/tokens/费用估算） |
| data/exports/\<accountId\>/ | 按账号隔离的 CSV 导出 |
| data/images/ | 海报/AI 图 |

批量上架 CSV 模板列：`标题, 价格, 成本, 图片路径, 目录, 平台, 账号(可空=当前), 描述(可空=AI生成)`。

---

## 7. 安全与合规

- 高危动作（上架/发货/调价/建计划/跨账号批量）默认预览，确认后才执行；确认记录写审计日志；
- 抓取拟人延迟（30-90s 随机）、低频、不绕过验证码；多账号额外错峰+加长间隔，界面明示多号风控风险；
- API key 与平台凭据仅存本地（settings.json / accounts.json，均 gitignore），绝不写日志；诊断日志导出时自动脱敏；
- 数据全部本地，不上云。

---

## 8. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 依赖 | 零三方依赖，Node ≥ 18 单运行时（朋友电脑装一个 Node 即可） |
| 易用性 | 全中文；不添加账号即体验模式；新用户 3 分钟完成首次比价；配置全部界面化 |
| 可用性 | 外部接口全部有兜底（LLM→规则、AI图→海报、实盘→体验提示、模型超时→重试1次→兜底） |
| 可维护 | API 名称集中于适配器常量；功能/平台/账号/模型四层正交 |

---

## 9. 开发计划（5 周交付）

| 里程碑 | 周期 | 内容 | 验收 |
| --- | --- | --- | --- |
| M1 体验模式+基座 | 第 1 周 | 8 大模块 + 设置页 + 模型接入层（四预设+自定义）+ 生图接入层，示例数据跑通 | 设置页切换 4 家 LLM 均出文案；关生图走海报；零经验 10 分钟完成首次比价 |
| M2 闲鱼多账号实盘 | 第 2 周 | 账号管理/健康面板/切换、Cookie 续期引导、上架、按账号擦亮/售罄上架、订单发货 | 双闲鱼号各自定时擦亮连续 3 天错峰执行、日志可查、互不干扰 |
| M3 拼多多实盘 | 第 3 周 | 多店铺授权接入：上架/订单/投流/调价；上架投流一体 | CSV 批量 20 商品单店一次成功；双店报表分列 |
| M4 定时任务+日报 | 第 4 周 | jobs 全量、分账号+汇总日报、用量统计、备份/诊断导出 | 日报含营收+模型花费；一键备份 zip 可还原 |
| M5 定制交付 | 第 5 周 | 预置朋友品类目录、智能体改名、一页纸使用手册、字段校准 | 朋友独立完成全流程无求助 |

工作量估算：约 22-28 人天（在 v1.0 基线上增量：账号层、模型层、设置页、jobs、实盘联调）。

---

## 10. 验收清单

1. 不添加账号启动，3 分钟内完成一次比价 + 一次导出；
2. 设置页完成全部配置（换 LLM/换生图/加账号），全程不碰配置文件；
3. 切换 LLM 供应商（四预设任一）后文案功能正常，失败走规则兜底不报错；
4. 生图 API 关闭或失败时自动回退本地海报；
5. 双闲鱼账号 + 双拼多多店铺并存：切换、数据隔离、跨账号批量带间隔；
6. 任何高危操作未确认时 100% 不触达平台；
7. CSV 批量上架 ≥20 条商品全部成功（含 AI 文案 + 自动做图）；
8. 各账号擦亮/售罄上架按各自计划执行且日志可查；
9. Cookie 过期在健康面板红点提示并有续期引导，不是裸报错；
10. 导出 CSV Excel 直开中文不乱码；日报含分账号+汇总+模型花费；
11. 一键备份 zip、一键导出脱敏诊断日志；
12. 朋友（零电商经验）不看手册能完成：配模型→加账号→上架→比价→发货→看日报。

---

## 11. 后续路线（仅备忘，不承诺）

- v1.2：日报推送微信/飞书；擦亮效果对比（擦亮前后曝光变化）；
- v1.3：商品搬家（闲鱼⇄拼多多互搬，含跨账号）；
- v2.0：账号风控加强（独立代理出口按账号配置，若朋友号数明显扩张）。
