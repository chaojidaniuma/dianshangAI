{
  "name": "@ecom-agent/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "node --watch src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ecom-agent/shared": "0.1.0",
    "better-sqlite3": "^13.0.3",
    "fastify": "^5.12.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.2"
  }
}
